import { NextFunction, Request, Response } from "express";

const getTaluk = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};

const getByIdTaluk = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};
