import { NextFunction, Request, Response } from "express";

const getTownPanchayat = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};

const getByIdTownPanchayat = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};

export { getTownPanchayat, getByIdTownPanchayat };
