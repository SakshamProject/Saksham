import { NextFunction, Request, Response } from "express";

const getMPConstituency = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};

const getByIdMPConstituency = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};
