import { NextFunction, Request, Response } from "express";

const getMunicipality = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};

const getByIdMunicipality = async (
  request: Request,
  response: Response,
  next: NextFunction
) => {};
